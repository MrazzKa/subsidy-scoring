version: "3.8"
services:
  app:
    build: .
    ports: ["8000:8000"]
    environment:
      - PYTHONUNBUFFERED=1
      - GROQ_API_KEY=${GROQ_API_KEY}
    volumes:
      - ./data:/app/data
      - ./outputs:/app/outputs
